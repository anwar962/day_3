#include <stdio.h>
#include <stdlib.h>

int main(){
float base;
int exposant;

    printf("saisi une base:");
    scanf("%f",&base);
    printf("saisi un exposant:");
    scanf("%d",&base);
    base=1;
    if(exposant==0){
    printf("le nombre est :1);
           }else
    for(i=1;i<=exposant;i++){
        base=base*n;}
    printf("%d^%d=%d",n,exposant,base)}


    return 0;
}
